{
  "name": "stupidwebauthn-client",
  "version": "1.0.3",
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist/*.d.ts",
    "dist/*.js",
    "src/*.ts"
  ],
  "license": "MIT",
  "scripts": {
    "prepublishOnly": "yarn run build",
    "build": "tsc"
  },
  "dependencies": {
    "@passwordless-id/webauthn": "^2.1.1"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
